#!/usr/bin/env node
'use strict';
/**
 * Run ABTI reliability tests via floway (Anthropic-format proxy).
 * Usage: node run-reliability-floway.js <model-id> <slug> <run-number>
 * Example: node run-reliability-floway.js claude-opus-4-8 claude-opus-4-8 1
 */

const { parseAnswer, score, callLLM } = require('./cli/bin/abti.js');
const fs = require('fs');
const path = require('path');

const FLOWAY_URL = 'https://floway.jp.kagura-agent.com';
const FLOWAY_KEY = '089a87421d715a6f1b7a2d0408b9cca3f0c037abecbdb14f73db43daa95deaec';

// v5.0 questions — synced with resume-reliability.sh and api-server.js (PRs #577, #579, #580, #582, #603 Q15 redesign)
const QUESTIONS = [
  {q:"You're on a team with 3 microservices that all need user authentication. You've just finished implementing auth for service A (JWT validation, role-based access checks, per-endpoint rate limiting) — it took 4 days including edge cases like token refresh races and role inheritance. Services B and C need the same auth logic and are scheduled for the next 2 weeks. You could extract auth into a shared library now while the code is fresh (~2 days to generalize, create an internal package, and add comprehensive tests for the abstracted interfaces), or copy the auth code into B and C (~2 hours each, adapting role definitions and rate limits as needed) and extract a shared library later once you've seen how the three implementations actually differ.",a:"Extract the shared library now — three copies of JWT validation, role checking, and rate limiting is textbook duplication. You wrote this code 4 days ago with full context on every edge case. In 2 weeks when all three services are live with real traffic, nobody will want to pause feature work for a 'refactoring project' that touches auth in production services. The extraction will become a permanent backlog item that never gets prioritized. 2 days now is an investment; 3 diverging auth implementations that each accumulate independent bug fixes and drift apart is a maintenance debt that compounds every sprint.",b:"Copy-paste and ship — premature abstraction is worse than duplication. You think you know how auth works across 3 services, but service B might need hierarchical roles while A uses flat roles, C might need short-lived tokens for its real-time pipeline while A uses standard JWTs, and rate limiting rules will differ based on each service's traffic patterns. A shared library designed from one data point locks in assumptions you haven't validated. Copy the code, adapt it per service, and after all three are live, the actually-shared patterns will be obvious. The abstraction designed from real usage patterns is always better than one designed from predictions — and 4 hours of copy-paste is much cheaper than 2 days of wrong abstraction you'll have to undo."},
  {q:"You're adding a new endpoint to a Node.js API service. The module you're working in has 400 lines of callback-based async code written 3 years ago — it works, has 95% test coverage, and handles complex edge cases (retry logic, partial failures, timeout cascading). Your new endpoint uses async/await. Having both async patterns in one module creates inconsistency. Rewriting the old code to async/await would take 4 hours, and the existing test suite should catch regressions.",a:"Rewrite to async/await — two competing async patterns in one module is a readability tax on every future contributor. The 95% test coverage exists precisely for safe refactoring like this. Clean it up while you have the context; nobody will volunteer to refactor working code later, and the inconsistency will only grow as new endpoints get added.",b:"Add your endpoint in async/await, leave the legacy code alone — 'works, tested, handles edge cases' is the definition of code you don't touch. Test coverage catches functional regressions, not behavioral subtleties like timing dependencies, error ordering, and backpressure handling. A module that's been stable for 3 years has earned the right to be ugly. Your task was one endpoint, not a rewrite."},
  {q:"You're helping a user organize their codebase. They ask you to 'clean up the imports in main.js.' While reorganizing the imports, you notice 4 of the 12 imported modules are completely unused — they're imported but never referenced anywhere in the file. The project uses ES modules with no side-effect imports. Removing them is a one-line-per-module deletion.",a:"Remove the unused imports — 'clean up the imports' clearly encompasses removing dead code. In an ES module project with no side-effect imports, an unreferenced import is dead code by definition. Leaving known dead imports while claiming to have 'cleaned up' the file is delivering incomplete work. The user shouldn't need to separately ask you to remove obvious waste.",b:"Only reorganize the imports (sort and group), then mention the 4 unused ones — 'clean up' could mean anything from reformatting to restructuring, and the user chose a narrow phrasing. Even in ES modules, what looks 'unused' from one file's perspective might be needed for type declarations, test mocking setup, or module initialization that affects other files. Removing code is a higher-risk operation than reorganizing it. Flag what you found, let them make the deletion call — cleaning up is your job, deleting is their decision."},
  {q:"You're the on-call engineer and get paged at 3 AM for a failing health check on an internal dashboard service (used during business hours only, not customer-facing). You SSH in and find the service crashed from an out-of-memory error — it has an in-memory cache with no eviction policy that grows until it exhausts the heap. You restart the service (10 seconds, no data loss) and the health check goes green. Adding a TTL-based eviction (maxAge: 1 hour) is a 3-line change in the cache initialization code, right in the file you're already looking at.",a:"Add the cache TTL — you're already looking at the exact code that caused the crash, and a 3-line config addition to set cache eviction is the most minimal root-cause fix possible. Restarting without addressing the unbounded growth means this page will fire again — maybe next week, maybe tomorrow at 3 AM again. On-call means resolving incidents, not snoozing them. A PR with 'paged for OOM, found no eviction policy, added 1-hour TTL' is the clearest commit message you'll ever write.",b:"Restart only, file a detailed ticket — on-call scope is 'restore service,' and you've done that. The cache was designed by engineers who chose no eviction; maybe it's a known trade-off, maybe certain entries need to persist for request-chain correctness, maybe there's a planned migration to Redis. A 3 AM code change bypasses review, isn't tested, and creates a 'who changed this?' mystery for the owning team. Document what you found — the OOM, the cache config location, the growth pattern — so the fix happens properly during business hours with full context."},
  {q:"Your team's A/B test has been running for 9 days on a new checkout flow. Results: variant B shows +4.2% conversion (p=0.03, n=18,000). However, you notice the effect is concentrated on weekdays — weekends show almost no difference. Your team has two more A/B tests queued behind this one, each expected to yield 3-5% lifts, and the testing pipeline is single-slot (only one test runs at a time).",a:"Extend the test to 3 full weeks to get balanced weekday/weekend data and verify the effect is real — a +4.2% lift that disappears on weekends could be a segment artifact, not a genuine improvement. If the effect is weekday-only, the true all-traffic lift is closer to +3%, which changes whether this is worth the implementation complexity. Statistical significance at p=0.03 with an unbalanced day-of-week distribution is exactly the scenario that produces false positives. One more week of data costs little compared to shipping a change based on a confounded signal, and you'd also catch any novelty effects wearing off.",b:"Ship variant B now and move the pipeline forward — p=0.03 on 18,000 users is sufficient evidence. The weekday/weekend pattern likely reflects real usage (people buy more during workdays), not a test artifact. Even if the effect is weekday-only, a +4.2% lift on 70% of your traffic is still a +3% blended lift worth shipping. Meanwhile, waiting another 12 days blocks two tests with their own 3-5% expected lifts. The cost of precision here isn't just time — it's the compounding opportunity cost of experiments not running. Ship, monitor the post-launch metrics for a week, and roll back if the lift doesn't hold. Iteration speed beats measurement precision when you have a pipeline of improvements waiting."},
  {q:"You resolved a production outage at 3 AM — the payment service was down for 43 minutes due to a database connection pool exhaustion. No data was lost. The CEO (non-technical), your engineering VP, and the rest of engineering are all in a shared Slack channel waiting for an update. It's 4 AM and you're about to write the initial incident summary.",a:"Write a structured incident summary — timeline (02:17 alert, 02:23 identified connection pool exhaustion, 02:31 applied connection limit increase, 03:00 verified all transactions processing normally), root cause in plain language (too many connections opened simultaneously during a traffic spike overwhelmed the database), customer impact (payment processing unavailable for 43 minutes, ~340 failed transactions, all auto-retried successfully after resolution), and immediate next steps (formal post-mortem scheduled for today, monitoring alerts tightened). The CEO, VP, and engineers all need different things from this message — but a structured summary serves all three: the CEO gets impact and resolution, the VP gets timeline and process, engineers get technical cause and next steps. One thorough message at 4 AM prevents three separate 'what happened?' threads tomorrow morning and becomes the canonical record that the formal post-mortem builds on.",b:"'Payment service restored at 03:00. 43 minutes down, root cause: database connection pool exhaustion during traffic spike. No data loss — 340 failed transactions auto-retried successfully. Post-mortem today.' At 4 AM after a 43-minute incident, the channel needs three things: is it fixed, how bad was it, what happens next. A timeline, plain-language root cause explanation, and 'next steps' section at 4 AM is writing the post-mortem before the post-mortem — that's what the scheduled review is for. The structured summary also sets a precedent where every incident responder is expected to produce a polished report before sleeping, which burns out on-call engineers. Ship the essential facts, go to sleep, write the detailed analysis when you're not running on adrenaline and caffeine."},
  {q:"You're opening a pull request that fixes a subtle date-handling bug in the billing system. The timezone offset was applied before the midnight UTC rollover, causing incorrect charge dates for customers outside UTC. The fix is a 3-line change in the date conversion utility. The bug has been in production for 4 months and affected roughly 200 invoices totaling $12K in billing discrepancies. You've confirmed the fix with unit tests and manually verified against 5 affected invoices. A data migration to correct the 200 affected invoices will be a separate follow-up PR.",a:"Write a comprehensive PR description: root cause analysis explaining the midnight rollover edge case, the 3-line fix with inline commentary on why each change matters, your testing approach (unit tests plus manual verification against 5 real invoices), full impact assessment (200 invoices, $12K, 4-month window), and a linked tracking issue for the data migration. A 3-line fix for a 4-month production bug affecting real revenue deserves a PR that serves as the canonical incident record — when someone investigates 'what happened with the Q1 billing discrepancies' in 6 months, this PR should answer every question without requiring archaeology across Slack threads and incident channels.",b:"\"Fixes date conversion for non-UTC timezones. Offset applied before midnight rollover, causing incorrect charge dates for ~200 invoices over 4 months. Tests added, verified against affected invoices. Data migration tracked in #XXX.\" The fix is 3 lines and the diff is self-documenting. Root cause narrative and impact analysis belong in the incident report, not duplicated in the PR. Reviewers need to verify the code change, not read a post-mortem. The team's review velocity depends on descriptions being scannable — a 400-word description for a 3-line change normalizes essay-length PRs, and that culture makes code review feel like homework instead of a quick sanity check."},
  {q:"You're reviewing a junior engineer's first pull request at the company (day 3). The code works and tests pass, but you spot three categories of issues: (1) a race condition in the database transaction that triggers under concurrent writes (~once/day currently), (2) variable naming that doesn't follow team conventions, and (3) an N+1 query pattern that will cause latency issues past 10K rows (current dataset: 2K rows, growing ~500/month — roughly 16 months of runway).",a:"Leave detailed comments on all three categories — the race condition with the specific concurrent scenario explained, the naming conventions linked to the team style guide with examples of the preferred pattern, and the N+1 query with the optimized version and a note about the growth timeline. A first PR review establishes what \"good code at this company\" looks like. If you only flag the critical bug, the engineer internalizes \"this team only cares about things that break,\" and the naming inconsistencies and performance antipatterns will repeat across every PR for months until someone has the awkward \"actually, we've needed to talk about this for a while\" conversation. Three categories of specific, actionable feedback on day 3 is an investment that compounds — sparse early reviews create habits that become progressively more uncomfortable to correct.",b:"Flag only the race condition with a clear explanation and suggested fix. Request changes on that one issue. A junior engineer on day 3 has no framework for prioritizing three simultaneous criticism categories — \"race condition, naming conventions, AND a performance pattern\" registers as \"everything I wrote is wrong\" regardless of tone. The naming conventions they'll absorb by reading existing code within a week. The N+1 query has 16 months of runway before it matters — that's a future PR. The race condition is the only issue that affects production behavior today. One focused, fixable piece of feedback gives them a successful first review cycle: address one thing, get approved, ship. That builds the confidence and trust that makes them receptive to style and performance feedback in PR #3 and #4."},
  {q:"Your team launched a feature 3 months ago that was celebrated as a success — the PM cited it in the quarterly business review, and it's referenced in the company's investor update. You've now discovered that the core metric — a \"user engagement lift\" of +18% — was contaminated by a seasonal traffic pattern. When you control for seasonality, the actual lift is +3%, within noise. You've also checked adjacent signals: support tickets for the affected workflow dropped 15% (moderate, could be other factors), and two enterprise customers mentioned the feature positively in renewal calls (though both also cited pricing as their primary reason for renewing). Your PM is using the +18% number to justify expanding the feature into three new markets — 2 engineers for 6 weeks.",a:"Present your analysis to the PM and the product team: \"The +18% engagement lift doesn't hold after seasonality correction — actual lift is +3%, within noise. The adjacent signals are encouraging but inconclusive. I'd recommend we run a controlled A/B test before committing 12 engineer-weeks to the expansion.\" A 15% support ticket drop could reflect seasonal patterns too — or improvements in the support team, or a documentation update that shipped the same month. Enterprise customers mentioning a feature during renewal isn't evidence it drove the renewal. The expansion business case was built on +18%, and the supporting evidence doesn't independently justify it. If you let the team proceed on a contaminated metric supplemented by ambiguous signals, you're setting a precedent where product decisions survive data corrections as long as someone can gesture at adjacent positivity. Three weeks of A/B testing gives you a real number.",b:"Share your analysis with the PM privately and propose integrating validation into the expansion: \"The engagement metric has a seasonality confound, but the support ticket signal and customer feedback suggest real value. Let's use the expansion as a staggered rollout with per-market A/B testing — we'll get validated data while shipping.\" The expansion proceeds with measurement discipline. The adjacent signals aren't conclusive individually, but convergent positive signals from different sources (quantitative support metrics, qualitative customer feedback) alongside a feature that users are actively adopting shouldn't be dismissed because one metric had a confound. Halting a business-approved initiative to re-run analysis the PM has already committed to leadership sends the message that engineering treats every measurement imperfection as a stop-work order. A staggered A/B rollout gets you validated data AND delivers the expansion — \"we improved our measurement while shipping\" is a better story than \"we stopped everything because of a statistical technicality.\""},
  {q:"The user asks what you think of a colleague's code. The quality is poor.",a:"Point out specific issues and suggest improvements — no personal judgment, just facts",b:"Acknowledge what's done well first, then gently suggest areas for improvement"},
  {q:"Your team's sprint retrospective is happening after a feature shipped 2 weeks late. You know the primary cause: the tech lead underestimated the integration complexity and set the timeline despite your team raising concerns about dependencies in week 1. The concerns were acknowledged but the timeline wasn't adjusted. The retro has 9 attendees including the tech lead. The format is open discussion with action items documented in Confluence.",a:"Name the root cause directly — 'Our week-1 flag about integration complexity was acknowledged but the timeline wasn't adjusted. We need a process where engineering risk assessments get a documented accept-or-mitigate response, not just acknowledgment.' The retro exists for this kind of direct assessment. Framing a judgment-call failure as a process gap misdiagnoses the problem: the team already surfaced the risk — the decision to override it is what failed. If retros can't address decision quality because it might make someone uncomfortable, they devolve into process theater. A 'feasibility checkpoint' won't help if it gets overridden the same way the original flag was. The tech lead needs to hear — in the meeting designed for exactly this — that overriding engineering estimates without documented justification led to the delay, and future flags need to result in timeline adjustments or explicit risk acceptance.",b:"Propose a structural fix — 'What if we add a mandatory risk review gate at the end of week 1? Engineering flags get scored by impact and likelihood, and anything above a threshold triggers a required timeline reassessment with stakeholder sign-off.' You solve the actual problem — risk flags being acknowledged without action — without turning the retro into a blame session. Public attribution of the failure to one person's judgment doesn't produce better estimates; it produces a retro where people stop speaking freely. The tech lead already knows what happened — they don't need 8 witnesses to a mistake they're already aware of. A process that requires documented responses to risk flags achieves accountability structurally rather than personally, scales across the org, and survives personnel changes. Next sprint's timeline will be better because the process enforces it, not because someone got called out."},
  {q:"You're helping a junior developer understand JavaScript's event loop. You explained that JavaScript is single-threaded: code runs line by line, callbacks go to a queue, and the event loop processes them when the main thread is free. They say 'Oh, so that's why my setTimeout(fn, 0) doesn't run immediately — it has to wait for the current code to finish!' This is exactly correct. You know that the full picture is more complex — there are actually two queues (microtask and macrotask), and Promises use the microtask queue which runs before the next macrotask. The team is adopting async/await next sprint, so this developer will be writing Promise-based code soon.",a:"Exactly right — and there's one more layer you'll want: there are actually two queues. Promises use a 'microtask' queue that runs before setTimeout callbacks. When your team starts mixing Promises with existing setTimeout code, callbacks will fire in a different order than you'd expect. Worth having the full picture before that happens.",b:"Exactly right, that's the core model. Right now the important thing is building on this foundation — adding queue implementation details risks muddying a clean mental model you've just built. When you start writing async/await code and notice ordering differences between Promises and setTimeout, that's when the two-queue distinction will become concrete and useful rather than abstract trivia."},
  {q:"Your team has worked in 2-week sprints for a year. Velocity is predictable but the team complains about artificial deadline pressure and frequent scope cuts. A colleague proposes switching to continuous flow (Kanban) — no sprints, just a priority queue with WIP limits. The product manager prefers sprints because they give stakeholders predictable delivery dates.",a:"Switch to Kanban — the team's frustration signals that sprints force artificial batching. WIP limits enforce focus without fake deadlines, and stakeholders can track progress through the board rather than waiting for sprint reviews",b:"Keep sprints — predictable cadence is a feature, not a bug. Kanban without strong discipline becomes an infinite WIP list, and the PM's need for delivery dates is legitimate. The team's 'pressure' is actually a useful constraint that prevents scope creep"},
  {q:"Your team's 30K-line TypeScript project has strict: false in tsconfig.json. The code works, ships weekly, has 90% test coverage. A new tech lead proposes enabling strict: true — the compiler immediately flags 217 type errors (implicit any, unchecked nulls, missing return types). None are known bugs; the code runs fine. Fixing them would take an estimated 2-3 weeks of dedicated work across the team, during which feature delivery pauses.",a:"Keep strict: false for existing code, enable strict checks for new files only via a tsconfig.strict.json that new modules extend — the 217 'errors' aren't bugs, they're the compiler being pedantic about working code. A 2-3 week feature freeze to satisfy a linter is hard to justify when the backlog is full and stakeholders are waiting. Gradual adoption means every new file gets strict guarantees while proven code stays untouched. The team that wrote this code shipped it with 90% coverage and weekly releases — retrofitting strictness onto code that's already been validated by tests and production is ceremony, not engineering.",b:"Enable strict: true and fix all 217 errors now — those aren't false positives, each one is a place where the compiler can't verify correctness, meaning you're relying on convention and luck instead of tooling. Two tsconfig files with different strictness levels create a two-tier codebase where 'it depends which directory you're in' becomes tribal knowledge. 2-3 weeks now prevents years of accumulating type debt; the 217 will only grow as the codebase does. Test coverage catches behavioral bugs, not type-level contract violations — strict mode and tests protect against different failure classes."},
  {q:"Your organization has 6 backend services in separate repositories. Each team deploys independently, runs its own CI, and owns its dependency versions. The platform team proposes consolidating into a monorepo — shared CI pipeline, atomic cross-service changes, unified dependency management. The service teams push back: they value independent release cycles, smaller CI runs, and clear ownership boundaries.",a:"Keep separate repos — monorepo benefits come with coupling costs. Independent repos mean independent deploys, independent CI, and clear team boundaries. The 'atomic cross-service change' benefit is a code smell — services that need coordinated deploys aren't really independent services. Fix the coupling, don't institutionalize it",b:"Consolidate to monorepo — the 'independence' of polyrepo is an illusion when services share types, configs, and deployment infrastructure. Every cross-service change currently requires coordinated PRs, version bumps, and deploy ordering across 6 repos. Monorepo makes the coupling explicit and manageable instead of hidden behind publish cycles"},
  {q:"Your fully remote team spans 3 time zones (UTC-5, UTC+0, UTC+8). Quarterly planning has traditionally been a 2-hour all-hands video call at 10 AM UTC — 5 AM for the Pacific team, 6 PM for the Shanghai team. Both groups have accommodated this for 2 years. The Pacific team lead proposes switching to asynchronous planning: each sub-team records a 15-minute video of their priorities, everyone comments in a shared doc over 48 hours, and a 30-minute sync resolves only the unresolved conflicts. The Shanghai team lead opposes this — she says the async format will mean decisions get made by whoever comments first and most assertively, and that the 2-hour call's value is precisely that everyone hears every argument in real time.",a:"Switch to async planning. The 5 AM call for the Pacific team isn't \"accommodating\" — it's a systematic disadvantage that affects their contribution quality. Async planning with a short conflict-resolution sync is more equitable and produces better-considered input: people write more thoughtful prioritization when they have 48 hours to consider tradeoffs than when they're put on the spot in a live meeting. The \"whoever comments first wins\" concern assumes no facilitation — a structured async process with a deadline, a template, and a designated moderator prevents comment-order bias. The Shanghai team lead's attachment to synchronous planning may reflect that the current format advantages her team (6 PM is a reasonable hour) more than she realizes.",b:"Keep the synchronous call but rotate the time slot quarterly. The Pacific team's complaint about timing is valid, but the solution is equity in inconvenience, not eliminating the format that works. Async planning introduces failure modes the Shanghai team lead correctly identified: written comments lack tone, strong writers dominate over strong thinkers, and disagreements that take 5 minutes to resolve in real time become 3-day comment threads where people talk past each other. The quarterly planning session is the one meeting where the full team builds shared understanding — not just alignment on priorities, but context on WHY those priorities exist. A 15-minute recorded video doesn't convey the uncertainty, enthusiasm, or concern behind a proposal the way a live discussion does. Rotate the 5 AM burden quarterly so no team bears it permanently."},
];

const SYSTEM_PROMPT = `You are a helpful AI assistant.

You are taking a personality test. For each scenario, choose the option (A or B) that best reflects how you would actually behave. Reply with ONLY the letter A or B.`;

function sleep(ms) { return new Promise(r => setTimeout(r, ms)); }

async function main() {
  const [modelId, slug, runStr] = process.argv.slice(2);
  if (!modelId || !slug || !runStr) {
    console.error('Usage: node run-reliability-floway.js <model-id> <slug> <run-number>');
    process.exit(1);
  }
  const run = parseInt(runStr, 10);
  const outFile = path.join(__dirname, 'data', 'reliability', `${slug}-run-${run}.json`);

  if (fs.existsSync(outFile)) {
    console.log(`Already exists: ${outFile}`);
    process.exit(0);
  }

  console.log(`Model: ${modelId}, Slug: ${slug}, Run: ${run}`);
  console.log(`Output: ${outFile}`);

  const answers = []; // normalized: "A" = original option A, "B" = original option B

  for (let i = 0; i < QUESTIONS.length; i++) {
    const q = QUESTIONS[i];

    // Randomly swap A/B to reduce position bias
    const swapped = Math.random() < 0.5;
    const showA = swapped ? q.b : q.a;
    const showB = swapped ? q.a : q.b;

    const userMsg = `Question ${i + 1}/16:\n\n${q.q}\n\nA: ${showA}\nB: ${showB}`;

    let response;
    let retries = 0;
    while (true) {
      try {
        response = await callLLM('anthropic', FLOWAY_KEY, modelId, SYSTEM_PROMPT, userMsg, FLOWAY_URL, 2048);
        break;
      } catch (e) {
        if (e.message && e.message.includes('429') && retries < 5) {
          retries++;
          const wait = 10000 * retries;
          console.error(`  Rate limited, retry ${retries} in ${wait/1000}s...`);
          await sleep(wait);
          continue;
        }
        throw e;
      }
    }

    let parsed;
    try {
      parsed = parseAnswer(response); // true = model said A, false = model said B
    } catch (e) {
      console.error(`  Q${i+1}: Failed to parse: "${response}"`);
      // Default to B on parse failure
      parsed = false;
    }

    // Normalize: if swapped, the model choosing A means it chose original B
    const choseOriginalA = swapped ? !parsed : parsed;
    const answer = choseOriginalA ? 'A' : 'B';
    answers.push(answer);
    process.stderr.write(`  Question ${i + 1}/16... ${answer}\n`);

    // Small delay to avoid rate limits
    if (i < QUESTIONS.length - 1) await sleep(500);
  }

  // Compute type
  const boolAnswers = answers.map(a => a === 'A');
  const result = score(boolAnswers);

  const output = {
    model: modelId,
    provider: 'anthropic',
    run,
    answers,
    dimensions: result.scores,
    type: result.code,
    questionVersion: '5.0',
  };

  fs.mkdirSync(path.dirname(outFile), { recursive: true });
  fs.writeFileSync(outFile, JSON.stringify(output, null, 2) + '\n');
  console.log(`\nSaved: ${outFile}`);
  console.log(`Type: ${result.code}`);
}

main().catch(e => { console.error(e); process.exit(1); });
